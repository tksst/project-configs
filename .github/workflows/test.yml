name: Lint

on:
  workflow_call:
  workflow_dispatch:
  pull_request:
  push:
    branches-ignore:
      - main

jobs:
  lint:
    name: Lint
    uses: tksst/github-actions-workflows/.github/workflows/nodejs-pnpm.yml@6527d452c88ace5c40d6c21e28b4fb7745e10501
    with:
      os: '["ubuntu-latest"]'
      node-versions: "[18]"
      pnpm-commands: '["lint"]'
      job-title: Lint
