name: Lint

on:
  workflow_call:
  workflow_dispatch:
  pull_request:
  push:
    branches-ignore:
      - main

jobs:
  lint:
    name: Lint
    uses: tksst/github-actions-workflows/.github/workflows/nodejs-pnpm.yml@dd41bcef2675d1198511e6ab772eb8d07a0ccd57
    with:
      os: '["ubuntu-latest"]'
      node-versions: "[18]"
      pnpm-commands: '["lint"]'
      job-title: Lint
